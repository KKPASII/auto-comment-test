# PR Review Log

- Repository: KKPASII/auto-comment-test
- PR Number: 3
- Title: refactor: 반복 함수 로직 수정
- Action: reopened
- Review At: 2026-03-21 21:38:09
- Diff URL: https://github.com/KKPASII/auto-comment-test/pull/3.diff

## GPT Review

코드 리뷰 코멘트:

1. **문제점**: 현재 이중 루프를 사용하여 문자열의 모든 문자를 비교하고 있습니다. 이 방식은 O(n^2)의 시간 복잡도를 가지며, 문자열의 길이가 길어질수록 성능에 큰 영향을 미칠 수 있습니다.

2. **개선 방향**: 중복 문자를 확인하는 더 효율적인 방법으로 `Set`을 사용하는 것을 제안합니다. `Set`을 사용하면 O(n)의 시간 복잡도로 중복 여부를 확인할 수 있습니다.

3. **개선된 코드 예시**:
   ```java
   import java.util.HashSet;

   public class ContainsDuplicateChar {
       public static boolean hasDuplicate(String str) {
           HashSet<Character> charSet = new HashSet<>();
           for (char c : str.toCharArray()) {
               if (!charSet.add(c)) {
                   return true; // 중복 발견
               }
           }
           return false; // 중복 없음
       }
   }
   ```

4. **결론**: 코드가 잘 작성되었으나, 성능 개선을 위해 중복 확인 로직을 최적화하는 것이 좋습니다. 위의 예시처럼 `Set`을 활용하면 더 효율적인 구현이 가능합니다.
